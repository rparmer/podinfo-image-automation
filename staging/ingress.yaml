apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-ingress
spec:
  rules:
  - host: podinfo-image-automation-staging.localhost
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: podinfo-image-automation
            port:
              name: http